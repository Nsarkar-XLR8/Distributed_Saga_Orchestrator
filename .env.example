# -----------------------------------------------------------------------------
# Kafka Configuration
# -----------------------------------------------------------------------------
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_CHAOS_BOOTSTRAP_SERVERS=localhost:9093

# -----------------------------------------------------------------------------
# Database Configuration - Order Service (PostgreSQL 16)
# -----------------------------------------------------------------------------
ORDER_DB_HOST=localhost
ORDER_DB_PORT=5432
ORDER_DB_USER=postgres
ORDER_DB_PASSWORD=postgres
ORDER_DB_NAME=order_db
ORDER_DB_URL=postgresql://postgres:postgres@localhost:5432/order_db

# -----------------------------------------------------------------------------
# Database Configuration - Inventory Service (PostgreSQL 16)
# -----------------------------------------------------------------------------
INVENTORY_DB_HOST=localhost
INVENTORY_DB_PORT=5433
INVENTORY_DB_USER=postgres
INVENTORY_DB_PASSWORD=postgres
INVENTORY_DB_NAME=inventory_db
INVENTORY_DB_URL=postgresql://postgres:postgres@localhost:5433/inventory_db

# -----------------------------------------------------------------------------
# Database Configuration - Payment Service (PostgreSQL 16)
# -----------------------------------------------------------------------------
PAYMENT_DB_HOST=localhost
PAYMENT_DB_PORT=5434
PAYMENT_DB_USER=postgres
PAYMENT_DB_PASSWORD=postgres
PAYMENT_DB_NAME=payment_db
PAYMENT_DB_URL=postgresql://postgres:postgres@localhost:5434/payment_db

# -----------------------------------------------------------------------------
# Database Configuration - CQRS Read Model (MongoDB 7.0)
# -----------------------------------------------------------------------------
READ_MODEL_MONGO_URI=mongodb://localhost:27017/read_model_db
READ_MODEL_DB_NAME=read_model_db

# -----------------------------------------------------------------------------
# Toxiproxy Configuration
# -----------------------------------------------------------------------------
TOXIPROXY_URL=http://localhost:8474

# -----------------------------------------------------------------------------
# Service Ports
# -----------------------------------------------------------------------------
ORDER_SERVICE_PORT=3001
INVENTORY_SERVICE_PORT=8082
PAYMENT_SERVICE_PORT=8081
READ_PROJECTION_SERVICE_PORT=3002
VISUALIZER_PORT=3000
